import { AdminTabTitleDirective } from './admin-tab-title.directive';

describe('AdminTabTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminTabTitleDirective();
    expect(directive).toBeTruthy();
  });
});
